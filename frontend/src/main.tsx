import { FormEvent, useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './style.css';

const API = import.meta.env.VITE_API_BASE_URL || 'http://127.0.0.1:8000';

type User = { id: string; username: string; role: string };
type KnowledgeBase = { id: string; name: string; description?: string; status?: string; current_version?: string };
type Document = { id: string; file_name: string; mime_type: string; status: string; content?: string | null; version?: number | null; content_hash: string };
type Job = { ingestion_job_id: string; document_id?: string; status: string; stage?: string; error?: string };
type EventData = Record<string, any>;

async function api<T = any>(path: string, options: RequestInit = {}): Promise<T> {
  const token = localStorage.getItem('token');
  const headers: Record<string, string> = { ...(options.body instanceof FormData ? {} : { 'Content-Type': 'application/json' }) };
  if (token) headers.Authorization = `Bearer ${token}`;
  const response = await fetch(`${API}${path}`, { ...options, headers: { ...headers, ...(options.headers as Record<string, string> || {}) } });
  const body = await response.json().catch(() => ({}));
  if (!response.ok) throw new Error(body.message || body.error?.message || `请求失败 (${response.status})`);
  return body.data as T;
}

function Login({ onLogin }: { onLogin: (user: User) => void }) {
  const [form, setForm] = useState({ username: '', password: '' }); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  const submit = async (e: FormEvent) => { e.preventDefault(); setLoading(true); setError(''); try { const data = await api<{ access_token: string; user: User }>('/api/v1/auth/login', { method: 'POST', body: JSON.stringify(form) }); localStorage.setItem('token', data.access_token); onLogin(data.user); } catch (err: any) { setError(err.message); } finally { setLoading(false); } };
  return <main className="login"><div className="brand-mark">AR</div><h1>Asphoif RAG</h1><p>企业级 RAG 开发测试控制台</p><form onSubmit={submit}><label>用户名<input required value={form.username} onChange={e => setForm({ ...form, username: e.target.value })} /></label><label>密码<input required type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} /></label><button disabled={loading}>{loading ? '登录中…' : '登录'}</button></form>{error && <div className="alert error">{error}</div>}</main>;
}

function KnowledgeSidebar({ kbs, selected, onSelect, onRefresh, onCreate, onDelete }: { kbs: KnowledgeBase[]; selected: string; onSelect: (id: string) => void; onRefresh: () => void; onCreate: (name: string) => void; onDelete: (id: string) => void }) {
  const [name, setName] = useState(''); return <aside className="sidebar"><div className="side-title"><h2>知识库</h2><button className="icon-button" onClick={onRefresh}>↻</button></div><div className="kb-list">{kbs.map(kb => <button key={kb.id} className={`kb-item ${selected === kb.id ? 'selected' : ''}`} onClick={() => onSelect(kb.id)}><span className="kb-dot" />{kb.name}</button>)}{!kbs.length && <p className="muted">暂无知识库</p>}</div><div className="create-kb"><input value={name} placeholder="新知识库名称" onChange={e => setName(e.target.value)} /><button onClick={() => { if (name.trim()) { onCreate(name.trim()); setName(''); } }}>创建</button></div>{selected && <button className="danger-button" onClick={() => onDelete(selected)}>删除当前知识库</button>}</aside>;
}

function DocumentsPanel({ kb }: { kb: string }) { const [docs, setDocs] = useState<Document[]>([]); const [doc, setDoc] = useState<Document>(); const load = async () => { if (!kb) return; const data = await api<{ items: Document[] }>(`/api/v1/documents?knowledge_base_id=${encodeURIComponent(kb)}`); setDocs(data.items || []); }; useEffect(() => { load(); setDoc(undefined); }, [kb]); return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">DOCUMENTS</span><h2>知识库文档</h2></div><button onClick={load}>↻</button></div><div className="documents-layout"><div className="document-list">{!docs.length && <p className="muted">暂无文档</p>}{docs.map(item => <button className={`document-item ${doc?.id === item.id ? 'selected' : ''}`} key={item.id} onClick={async () => setDoc(await api<Document>(`/api/v1/documents/${item.id}`))}><b>{item.file_name}</b><small>{item.mime_type} · {item.status}</small></button>)}</div><div className="document-content">{doc ? <><h3>{doc.file_name}</h3><small>版本 {doc.version ?? '—'} · {doc.content_hash}</small><pre>{doc.content || '暂无内容'}</pre></> : <div className="empty"><span>▤</span><p>选择文档查看内容</p></div>}</div></div></div>; }

function ChatPanel({ kb, onTrace }: { kb: string; onTrace: (data: EventData) => void }) {
  const [query, setQuery] = useState(''); const [answer, setAnswer] = useState(''); const [citations, setCitations] = useState<any[]>([]); const [busy, setBusy] = useState(false); const [error, setError] = useState('');
  const send = async () => { if (!query.trim() || !kb) return; const current = query.trim(); setQuery(''); setAnswer(''); setCitations([]); setError(''); setBusy(true); try { const response = await fetch(`${API}/api/v1/chat/completions`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${localStorage.getItem('token')}` }, body: JSON.stringify({ query: current, knowledge_base_id: kb }) }); if (!response.ok || !response.body) throw new Error('Chat 请求失败'); const reader = response.body.getReader(); const decoder = new TextDecoder(); let buffer = ''; while (true) { const part = await reader.read(); if (part.done) break; buffer += decoder.decode(part.value, { stream: true }); const frames = buffer.split('\n\n'); buffer = frames.pop() || ''; for (const frame of frames) { const event = frame.match(/^event: (.+)$/m)?.[1]; const raw = frame.match(/^data: (.+)$/m)?.[1]; if (!raw) continue; const data = JSON.parse(raw); onTrace({ ...data, event }); if (event === 'delta' && data.text) setAnswer(v => v + data.text); if (event === 'citation') setCitations(v => [...v, data]); if (event === 'error') setError(data.message || '生成失败'); } } } catch (err: any) { setError(err.message); } finally { setBusy(false); } };
  return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">ONLINE QA</span><h2>Chat 流式问答</h2></div><span className={`status-pill ${busy ? 'running' : ''}`}>{busy ? '生成中' : '就绪'}</span></div><div className="answer-box">{answer || <span className="placeholder">输入问题，验证检索、生成与引用链路…</span>}</div>{citations.length > 0 && <div className="citations"><h3>引用来源</h3>{citations.map((c, i) => <div className="citation" key={`${c.chunk_id}-${i}`}><b>[{i + 1}]</b><span>{c.file_name || c.document_id || c.chunk_id}</span><code>{c.chunk_id}</code></div>)}</div>}<textarea value={query} onChange={e => setQuery(e.target.value)} onKeyDown={e => { if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) send(); }} placeholder="例如：退款多久到账？（Ctrl/⌘ + Enter 发送）" /><button className="primary" disabled={busy || !kb || !query.trim()} onClick={send}>{busy ? '生成中…' : '发送问题'}</button>{error && <div className="alert error">{error}</div>}</div>;
}

function UploadPanel({ kb, jobs, onUpload }: { kb: string; jobs: Job[]; onUpload: (file: File) => void }) { return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">INGESTION</span><h2>文档上传与任务</h2></div></div><label className="dropzone"><span className="upload-icon">↑</span><b>选择 Markdown 或 TXT 文件</b><small>上传后自动创建入库任务并轮询状态</small><input type="file" accept=".md,.markdown,.txt,text/plain,text/markdown" disabled={!kb} onChange={e => { const file = e.target.files?.[0]; if (file) onUpload(file); e.currentTarget.value = ''; }} /></label>{!kb && <p className="muted">请先在左侧选择知识库</p>}<div className="job-list">{jobs.map(job => <div className="job-card" key={job.ingestion_job_id}><div><b>{job.document_id || '新文档'}</b><code>{job.ingestion_job_id}</code></div><span className={`job-status ${job.status.toLowerCase()}`}>{job.status}{job.stage ? ` · ${job.stage}` : ''}</span>{job.error && <small className="error">{job.error}</small>}</div>)}</div></div>; }

function SearchPanel({ kb, onTrace }: { kb: string; onTrace: (data: EventData) => void }) { const [query, setQuery] = useState(''); const [result, setResult] = useState<any>(); const [loading, setLoading] = useState(false); const run = async () => { if (!kb || !query.trim()) return; setLoading(true); try { const data = await api('/api/v1/retrieval/search', { method: 'POST', body: JSON.stringify({ query: query.trim(), knowledge_base_id: kb }) }); setResult(data); onTrace({ event: 'retrieval', ...data }); } catch (e: any) { onTrace({ event: 'error', message: e.message }); } finally { setLoading(false); } }; return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">RETRIEVAL</span><h2>检索调试</h2></div></div><textarea value={query} onChange={e => setQuery(e.target.value)} placeholder="输入检索问题…" /><button className="primary" disabled={!kb || loading || !query.trim()} onClick={run}>{loading ? '检索中…' : '执行检索'}</button>{result && <div className="retrieval-result"><div className="metric-row"><Metric label="状态" value={result.status} /><Metric label="Dense" value={result.dense?.length ?? 0} /><Metric label="Sparse" value={result.sparse?.length ?? 0} /><Metric label="融合" value={result.fused?.length ?? 0} /></div><pre>{JSON.stringify(result, null, 2)}</pre></div>}</div>; }
function Metric({ label, value }: { label: string; value: any }) { return <div className="metric"><span>{label}</span><b>{value}</b></div>; }
function TracePanel({ trace }: { trace?: EventData }) { return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">OBSERVABILITY</span><h2>Trace 专用视图</h2></div></div>{trace ? <><div className="trace-grid"><Metric label="事件" value={trace.event || '—'} /><Metric label="序号" value={trace.seq || '—'} /><Metric label="状态" value={trace.status || '—'} /><Metric label="Trace ID" value={trace.trace_id || '—'} /></div><pre>{JSON.stringify(trace, null, 2)}</pre></> : <div className="empty"><span>◎</span><p>完成一次 Chat 或检索后，这里会显示 Trace 摘要。</p></div>}<p className="muted">后端 Trace 查询接口接入后，此处可扩展为历史 Trace 检索。</p></div>; }

function EvaluationPanel() { const [snapshots,setSnapshots]=useState<any[]>([]); const [runs,setRuns]=useState<any[]>([]); const [question,setQuestion]=useState(''); const [gold,setGold]=useState(''); const [selected,setSelected]=useState(''); const [results,setResults]=useState<any>(); const [message,setMessage]=useState(''); const [judgeBase,setJudgeBase]=useState(''); const [judgeKey,setJudgeKey]=useState(''); const [judgeModel,setJudgeModel]=useState(''); const [review,setReview]=useState(''); const load=async()=>{try{const [s,r]=await Promise.all([api<any>('/api/v1/evaluations/snapshots'),api<any>('/api/v1/evaluations/runs')]);setSnapshots(s.items||[]);setRuns(r.items||[])}catch(e:any){setMessage(e.message)}}; useEffect(()=>{load()},[]); const create=async()=>{try{const d=await api<any>('/api/v1/evaluations/snapshots',{method:'POST',body:JSON.stringify({name:'回归数据集',cases:[{question,gold_chunk_ids:gold.split(',').map(x=>x.trim()).filter(Boolean)}]})});setSelected(d.id);setQuestion('');setGold('');setMessage('快照已创建');load()}catch(e:any){setMessage(e.message)}}; const publish=async()=>{if(selected){await api(`/api/v1/evaluations/snapshots/${selected}/publish`,{method:'POST'});setMessage('快照已发布');load()}}; const run=async()=>{if(selected){try{const d=await api<any>('/api/v1/evaluations/runs',{method:'POST',body:JSON.stringify({snapshot_id:selected,manifest:{judge_base_url:judgeBase||undefined,judge_api_key:judgeKey||undefined,judge_model:judgeModel||undefined,judge_prompt_version:'ragas-default-v1'}})});await api(`/api/v1/evaluations/runs/${d.id}/execute`,{method:'POST'});setMessage('评估完成');load()}catch(e:any){setMessage(e.message)}}}; const inspect=async(id:string)=>setResults(await api(`/api/v1/evaluations/runs/${id}/results`)); return <div className="panel"><div className="panel-heading"><div><span className="eyebrow">EVALUATION</span><h2>评估与回归测试</h2></div><button onClick={load}>↻</button></div><div className="eval-grid"><div><input value={question} onChange={e=>setQuestion(e.target.value)} placeholder="问题"/><input value={gold} onChange={e=>setGold(e.target.value)} placeholder="gold chunk id（逗号分隔）"/><button className="primary" onClick={create} disabled={!question.trim()}>创建快照</button><select value={selected} onChange={e=>setSelected(e.target.value)}><option value="">选择快照</option>{snapshots.map(s=><option key={s.id} value={s.id}>{s.name} · {s.status}</option>)}</select><button onClick={publish} disabled={!selected}>发布快照</button></div><div><input value={judgeBase} onChange={e=>setJudgeBase(e.target.value)} placeholder="Judge Base URL（可选，默认读取.env）"/><input type="password" value={judgeKey} onChange={e=>setJudgeKey(e.target.value)} placeholder="Judge API Key（可选）"/><input value={judgeModel} onChange={e=>setJudgeModel(e.target.value)} placeholder="Judge 模型（可选）"/><button className="primary" onClick={run} disabled={!selected}>创建并执行评估</button>{runs.map(r=><div className="job-card" key={r.id}><span>{r.id}</span><span className="job-status">{r.status}</span><button onClick={()=>inspect(r.id)}>结果</button></div>)}</div></div>{results&&<><div className="metric-row">{Object.entries(results.aggregate_metrics||{}).filter(([k])=>k.includes("faith")||k.includes("relev")||k.includes("precision")||k.includes("recall")||k.includes("correct")).map(([k,v])=><Metric key={k} label={k} value={String(v)}/>)}</div>{results.items?.map((item:any)=><div className="job-card" key={item.id}><span>{item.case_id}</span><span className="job-status">{item.status}</span><input value={review} onChange={e=>setReview(e.target.value)} placeholder="人工评分 JSON"/><button onClick={async()=>{try{await api(`/api/v1/evaluations/runs/${item.run_id}/results/${item.id}/review`,{method:"POST",body:JSON.stringify({scores:{overall:Number(review)||0},reviewer_note:"前端评审"})});setMessage("人工评分已保存")}catch(e:any){setMessage(e.message)}}}>提交人工评分</button></div>)}</> }{message&&<p className="muted">{message}</p>}</div> }function App() { const [user, setUser] = useState<User>(); const [kbs, setKbs] = useState<KnowledgeBase[]>([]); const [kb, setKb] = useState(''); const [tab, setTab] = useState('chat'); const [jobs, setJobs] = useState<Job[]>([]); const [trace, setTrace] = useState<EventData>(); const [error, setError] = useState(''); const load = async () => { try { const data = await api<{ items: KnowledgeBase[] }>('/api/v1/knowledge-bases'); setKbs(data.items); if (!kb && data.items[0]) setKb(data.items[0].id); } catch (e: any) { setError(e.message); } }; useEffect(() => { if (user) load(); }, [user]); const upload = async (file: File) => { try { const form = new FormData(); form.append('file', file); const job = await api<Job>(`/api/v1/knowledge-bases/${kb}/documents`, { method: 'POST', body: form }); setJobs(v => [job, ...v]); const poll = async () => { try { const state = await api<Job>(`/api/v1/ingestion-jobs/${job.ingestion_job_id}`); setJobs(v => v.map(item => item.ingestion_job_id === job.ingestion_job_id ? { ...item, ...state } : item)); if (!['READY', 'FAILED'].includes(state.status)) window.setTimeout(poll, 2000); } catch (e: any) { setError(e.message); } }; poll(); } catch (e: any) { setError(e.message); } }; if (!user) return <Login onLogin={setUser} />; const tabs = [['chat', 'Chat 问答'], ['upload', '文档入库'], ['documents', '文档浏览'], ['search', '检索调试'], ['trace', 'Trace 观测'], ['evaluation', '评估回归']]; return <main className="app-shell"><header className="topbar"><div className="logo"><span>AR</span><b>Asphoif RAG</b></div><div className="top-actions"><span>{user.username} · {user.role}</span><button onClick={() => { localStorage.clear(); setUser(undefined); }}>退出登录</button></div></header><div className="workspace"><KnowledgeSidebar kbs={kbs} selected={kb} onSelect={setKb} onRefresh={load} onDelete={async id => { if (window.confirm('确定删除当前知识库吗？')) { try { await api(`/api/v1/knowledge-bases/${id}`, { method: 'DELETE' }); setKb(''); await load(); } catch (e: any) { setError(e.message); } } }} onCreate={async name => { try { await api('/api/v1/knowledge-bases', { method: 'POST', body: JSON.stringify({ name }) }); load(); } catch (e: any) { setError(e.message); } }} /><section className="content"><div className="content-tabs">{tabs.map(([id, label]) => <button className={tab === id ? 'active' : ''} onClick={() => setTab(id)} key={id}>{label}</button>)}</div><div className="kb-banner">当前知识库：<b>{kbs.find(x => x.id === kb)?.name || '未选择'}</b><span>{kb || '请选择知识库后开始测试'}</span></div>{tab === 'chat' && <ChatPanel kb={kb} onTrace={setTrace} />}{tab === 'upload' && <UploadPanel kb={kb} jobs={jobs} onUpload={upload} />} {tab === 'documents' && <DocumentsPanel kb={kb} />}{tab === 'search' && <SearchPanel kb={kb} onTrace={setTrace} />}{tab === 'trace' && <TracePanel trace={trace} />}{tab === 'evaluation' && <EvaluationPanel />} {error && <div className="alert error global-error">{error}<button onClick={() => setError('')}>×</button></div>}</section></div></main>; }
createRoot(document.getElementById('root')!).render(<App />);




